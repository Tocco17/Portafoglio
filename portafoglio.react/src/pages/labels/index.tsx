import { LoaderFunctionArgs } from "react-router-dom"

export const labelPageLoader = async ({request, params}: LoaderFunctionArgs) => {
	
	
	return true
}

export const LabelPage = () => {
	return (<>
	LABEL
	</>)
}